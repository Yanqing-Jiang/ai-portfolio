
## October 2, 2025 - Visualization Parity & Responses Search Hardening
- **Status: Completed October 2, 2025**
  - Stabilize Responses API web search with a harness and richer telemetry.
  - Unify planner, single-agent, and multi-agent tool bundles so `analysis_complete` carries chart specs, stock widgets, and web context.
  - Fix multi-agent orchestration crash on the planner/web agents and expose the consolidated bundle to downstream summaries.
  - Align frontend stack order (Interactive Viz → TradingView → linked analysis → collapsed SQL) with Markdown citations.
  - Added regression checks (single-agent + multi-agent flows, web retriever harness) and documented the optional `ANALYTICS_ENABLE_WEB_SEARCH` flag.
