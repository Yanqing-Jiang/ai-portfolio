single agent: 
Make sure 1. market research: 2 questions can run concurrently 2. stock chart generation 3. sql events(intent, planning, generation, sql chart) can run concurrently at the same time for these 3 tasks. 
Make sure Financial Analysis card only show up once, and it has information from 1. sql data 2. online search data 3. stock data
single agent revision:
agent make decision on what part of the analysis to rerun, make sure chart re-run is for sql chart. 


multi-agent: 
Make sure sql, stock and web research do not runed twice. Make sure 1. market research: 2 questions can run concurrently 2. stock chart generation 3. sql events(intent, planning, generation, sql chart) can run concurrently at the same time for these 3 tasks. 
After orchestration, the supervisor emits a sanitized `cohesive_result` containing:

  - Final analysis with: 1. sql data 2. online search data 3. stock data

all the following could be emitted by specialists before supervisor's final analysis, by their own cards/chat result (make sure to show that in chat):

  - stock Chart
  - Web Chart
  - SQL Chart
  - Generated SQL Card

Make sure supervisor agent manage these specailists, make decision on what part of the analysis to rerun, make sure chart re-run is for sql chart:
  - 1. intent_liaison (LLM) – clarifies any missing slots before planning proceeds.  
  - 2. SQL chain (sql_planner + sql_generator + sql_validator + sql_executor) – produces validated SQL, columns, sample rows, and counts in sequence 
  - 3. viz_designer (LLM) – assembles or patches the ECharts visualization spec and enforces a non-null primary series 
  - 1. market_agent (deterministic with optional LLM commentary) – prepares the TradingView widget for requested symbols 
  - 1. web_research_agent (LLM) – normalizes search snippets into the shared web context
  - 4. insight_reviewer (LLM) – composes the TL;DR summary and supporting bullets from the gathered artifacts 
Make sure the sequence of running these specialists are 1->4 there are 3 1s because they can be run concurrently
