# Agent Process Ledger Investigation (November 13, 2025)

## Completed Work
- **Ledger reconstruction & telemetry correlation:** Replayed session `35b4199a-b135-40ba-a4e8-5271f3722d50` against `docs/agent-process-ledger (28-29).json` and `agentic-roadmap.md`, aligning `backend/backend_uvicorn.log` timestamps (15:59:32–15:59:55) so every telemetry event maps to the failed revision request.
- **Structured intent resolver guardrails:** `_normalize_schema_for_responses` now respects `_should_require_property`, preserves optional dictionary fields, emits `intent_resolution_schema_error`, and is covered by `backend/tests/analytics/test_unified_responses_client.py`, eliminating the Responses API 400 loop.
- **Snapshot invariant instrumentation (Plan Item #2 execution):** SQL, dataset preview, market, and web adapters immediately call `persist_lane_receipt(...)`, the manifest builder blocks publication until the quartet of receipts arrives, telemetry fires `analysis_lane_missing_artifact`/`analysis_inputs_missing`/`analysis_inputs_manifest_sealed`, and receipts now store normalized pointers for deterministic replays.
- **Re-derive revision workflow (Plan Item #3 execution):** Added `hydrate_inputs_manifest(session_id)` plus `analysis_writer.derive_from_inputs(...)`, letting `analytics_memory_workflow` auto-rerun sealed manifests, merge optional `web_refresh=true` snippets, and emit `analysis_rerun_started`/`analysis_rerun_completed` telemetry without relying on `last_analysis` text.
- **Planner/executor web receipt sealing:** `_persist_session_state(..., record_web=True)` alongside `_web_search_phase` and `refresh_web_lane` now serializes `web_search` payloads immediately (`backend/analytics/flows/planner_executor.py:3067-5170`), preventing manifests from stalling on missing web receipts.
- **Single-agent manifest sealing:** `_record_lane_outputs` copies `last_sql`, dataset previews (or `sample_rows`), market snapshots, and sanitized web snippets into the session snapshot; `test_single_agent_seals_manifest_when_inputs_available` inside `backend/tests/analytics/test_session_state_receipts.py` guards the behavior so revisions never regress.
- **Ops tooling & validation:** Fast-path smoke tests cover telemetry for both `single-agent` and `planner-executor` flows, and `scripts/backfill_manifest_receipts.py` hydrates historical sessions before enabling the manifest contract.
- **Root causes documented:** Captured the race between revision requests and baseline persistence, the structured resolver schema bug, and documentation drift in `docs/agentic-roadmap.md`, giving Ops a consistent narrative for ledger regressions.

## Remaining Work
- **Finalize planner/executor manifest sealing:** Implement the dataset-preview persistence path that runs immediately after `_set_sql_execution_artifact`, add a `record_dataset_preview` flag to `_persist_session_state`, extend `backend/tests/analytics/test_session_state_receipts.py` with the planner/executor regression case, and emit `analysis_lane_missing_artifact` warnings when SQL rows exist but no dataset receipt was persisted.
- **Broaden persistence safeguards across controllers:** Ensure `_persist_session_state` serializes dataset previews even when `record_sql=False`, teach every `SingleAgentController` call site to pass the explicit lane flags, and add a planner-only regression test so `_planner._persist_session_state(record_artifacts=True)` seals manifests without the agent runtime.
- **Multi-agent web lane refresh fix:** Update `_needs_web_refresh` and `MultiAgentRefinementFlow.run_web_refresh` to consider manifest freshness plus lane-age thresholds, hydrate manifests inside `analytics_memory_workflow` before emitting `follow_up_route`, and log `multi_agent.web_refresh_skipped` whenever the heuristic declines a rerun even though the UI requested refreshed context.
- **Frontend + SSE consistency:** Gate revisions on `analysis_inputs_manifest.status`, emit a `baseline_still_streaming` SSE event instead of `cannot_revise` when the manifest is in-flight, and update `components/analytics/hooks/useAnalyticsMemoryStream.ts` to coalesce card stacks by `(session_id, revision_id)` so stalled revisions update the existing column instead of duplicating it.
- **Receipts + telemetry reliability hygiene:** Finish the ongoing work to emit `intent_resolution_schema_error` + `analysis_inputs_missing` metrics with component context and refresh `docs/agentic-roadmap.md` so it reflects the re-derive strategy that is now live.
- **Validation capture:** After manifest gating lands, capture a fresh ledger run (e.g., `docs/agent-process-ledger - 2025-11-12T235946.374.json`) to prove the UI card coalescing + manifest sealing fixes eliminate duplicate stacks.

## Unresolved Questions
- Should follow-up requests be queued server-side until `lane_ready` arrives, or should the client automatically retry when it receives the "baseline still finishing" banner?