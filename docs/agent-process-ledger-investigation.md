# Agent Process Ledger Investigation (November 14, 2025)

## Vision
- Keep the November 7, 2025 Agentic Analytics Roadmap goals front and center by documenting how each ledger fix or test keeps revisions fully agent-driven while fresh runs stay deterministic.
- Ensure telemetry, receipts, and manifests stay provably in sync by replaying real sessions, capturing gaps, and logging which remediation tasks are complete so Ops and UI teams can audit evidence without rereading raw ledgers.
- Maintain a single queue for the remaining True Agentic Revision Plan deliverables (controller, UI, telemetry, and tooling updates) so engineering, product, and support execute against one authoritative source.

## Completed Work
### Ledger + Telemetry Stabilization
- **Ledger replay plus documentation:** Replayed session `35b4199a-b135-40ba-a4e8-5271f3722d50` against `docs/agent-process-ledger (28-29).json`, lined up telemetry with `backend/backend_uvicorn.log` (15:59:32-15:59:55), and captured the narrative here and in `docs/agentic-roadmap.md` so the failure mode stays auditable.
- **Structured intent resolver guardrails:** `_normalize_schema_for_responses` now respects `_should_require_property`, preserves optional dictionary fields, emits `intent_resolution_schema_error`, and is covered by `backend/tests/analytics/test_unified_responses_client.py`, eliminating the Responses API 400 loop.
- **Manifest instrumentation and single-agent sealing:** SQL, dataset preview, market, and web adapters call `persist_lane_receipt(...)`, manifests block until all components arrive, telemetry emits `analysis_lane_missing_artifact` / `analysis_inputs_missing` / `analysis_inputs_manifest_sealed`, and `_record_lane_outputs` keeps cached SQL/dataset/web artifacts in sync; `test_single_agent_seals_manifest_when_inputs_available` covers the regression.
- **Planner/executor web sealing and ops validation:** `_persist_session_state(..., record_web=True)` together with `_web_search_phase` and `refresh_web_lane` now writes `web_search` receipts immediately, while `scripts/backfill_manifest_receipts.py` plus the smoke suite cover both single-agent and deterministic planner runs.
- **Dataset receipt normalization and telemetry coverage:** Added `normalize_row_count` in `backend/analytics/core/session_state.py`, taught `PlannerExecutorFlow._persist_session_state` to coerce string row counts, store sanitized previews, and log `raw_row_count` metadata, and hydrated revision replay with the same helper. New tests (`test_dataset_preview_receipt_written_for_string_row_count`, `test_dataset_preview_warning_for_non_numeric_row_count`) plus the manifest and revision routing suites (`backend/tests/analytics/test_session_state_receipts.py`, `backend/tests/analytics/test_revision_routing.py`) now pass, demonstrating the ledger's `follow_up_route/cannot_revise` banner clears once dataset receipts are sealed.
- **OpenAI Agents SDK integration (single-agent flow):** `AgentRuntime` + `AgentMemory` now drive the plan-act-observe loop for the single-agent flow via OpenAI Agents SDK v0.5.0, persisting plan state, tool receipts, and agent run metadata into `SessionStateSnapshot.tool_cache["agent"]` so agent turns remain aligned with planner manifests and revision routing.
- **Dataset receipts, manifest hydration, and agent lifecycle remediation:** Planner SQL lanes now call `_persist_session_state(..., record_dataset_preview=True)` on every completion, SingleAgentController persists agent-generated datasets without duplicating artifacts, `_persist_session_state` lets downstream lanes backfill previews, and `_hydrate_inputs_manifest` plus the new `reset_session`/idle-timeout logic keep manifests and agent caches fresh. Tests `test_manifest_hydration_seals_pending_dataset_receipts` and `test_manifest_pending_revision_never_cannot_revise` guard the regression.
- **Snapshot hydration for missing receipts (Nov 14 @ 22:45Z):** Ledger `docs/agent-process-ledger - 2025-11-14T174323.545.json` still surfaced `cannot_revise` because Redis held SQL/web artifacts but no `planner_dataset_preview`/`planner_stock_widget`. `SessionStateSnapshot.ensure_dataset_preview_from_revision()` now backfills both receipts from either `analytics.revision_snapshot` *or* the cached `analytics.artifacts.sql_execution`/`market.snapshot`, and new tests (`test_dataset_preview_backfilled_from_artifacts`, `test_market_receipt_backfilled_from_artifacts`) prove manifests seal automatically once artifacts exist.
- **Forced web refresh for analysis revisions:** `_stream_revision_fast_path` (plus the fallback workflow branch) now synthesizes a minimal `RevisionDirective` whenever analysis/web lanes refresh, both SingleAgentController and MultiAgentFlow propagate `revision_inputs_plan={"sql": "reuse", "web": "refresh"}` into `ctx.force_revision_refresh`, `WebRetrieverAdapter.execute` treats that flag the same as `revision_search_topics`, and `_planner._persist_session_state(..., record_web=True)` seals the refreshed snippets before `analysis_revision` returns.

- **Flow-mode telemetry playback (Nov 17, 2025 @ 06:55 UTC):** useAnalyticsMemoryStream now normalizes the mode/flow_mode fields emitted in docs/agent-process-ledger - 2025-11-17T013209.805.json and docs/agent-process-ledger - 2025-11-17T013324.509.json, stores the active 	elemetryFlowMode, and feeds it into ProcessPanel so the ledger UI renders only the revision stack instead of replaying the entire planner pipeline; Vitest suites components/analytics/hooks/useAnalyticsMemoryStream.test.tsx and components/analytics/memory/Page.test.tsx replay both ledgers to cover the regression.
- **Flow-mode telemetry surfaces (Nov 17, 2025 @ 07:25 UTC):** LiveArtifacts, SingleAgentFanoutCanvas, and the SQL analytics panel now consume telemetryFlowMode so flow badges, visualization themes, and server-driven ProcessPanel states all match the real agent mode; useAnalyticsSqlStream emits the detected flow, and a Vitest replay drives docs/agent-process-ledger - 2025-11-17T013324.509.json to guard multi-agent regressions.

### Agentic Revision Decision Notes (November 16, 2025)
- **Lane-decision schema:** The new `lane_decision` tool in the `agentic_revision` plan template emits `{"lane_decision": "chart"|"narrative", "questions": {...}}`. `AgentsStreamBridge` forwards that receipt to `AgentMemory.record_lane_decision`, and `AgentRuntime.run` blocks `PlanState.mark_finished("apply_revision_lane")`/raises `analysis_revision_blocked` if no decision arrives.
- **Agent invocation flow:** `_agent_run_stage` remains the unified runtime entry point. `run_analysis_refresh()` executes the `agentic_revision` plan first, waits for `AgentMemory.get_lane_decision()`, persists the outcome, and only then branches into targeted chart/narrative + `run_web_refresh` paths so SSEs, telemetry, and manifests stay aligned.
- **Gemini questions for fresh + revision web paths:** `services/response_search.py` now returns two prompts (user focus + industry context). Both `WebRetrieverAdapter.execute` and `_web_search_phase` emit the bundle, persist it via `SessionStateSnapshot.record_web_research_questions`, and include the questions in every `web_ready`/`web_revision_ready` SSE so revisions reuse the same hints.
- **Frontend data model:** `WebSearchResult` / `normalizeWebContext` absorb the new question pair, and `WebSearchCard` surfaces them so the inspector + web research widget display "User question" vs. "Industry context" without new streams. Because `webSearch` already powers revision prompts, no extra state slices are required.

### True Agentic Revision Plan Progress (Nov 17, 2025)
- **Single-agent revision orchestration:** `run_analysis_refresh()` and `analysis_revision()` now honor the normalized plan, reuse Gemini bundles from AgentMemory, gate web refreshes on `{lane, web}` hints, emit `revision_questions_ready` specialist cards, and persist `revision_inputs_outcome` for the ledger (`backend/analytics/flows/single_agent_tools.py`, `components/analytics/hooks/useAnalyticsMemoryStream.ts`).
- **Multi-agent revision parity:** Multi-agent flows mirror the same plan-driven lane selection, stream the Gemini bundles to specialists, emit the new SSE/card payloads, and persist revision outcomes so supervisor telemetry matches planner executions (`backend/analytics/flows/multi_agent.py`).
- **Classifier + UI awareness of Gemini prompts:** `FollowUpClassifier.classify()` now consults cached revision bundles before keyword heuristics, and the frontend stream hook/banners surface the question pair plus a dedicated Revision Questions specialist card (`backend/analytics/routing/follow_up_classifier.py`, `components/analytics/hooks/useAnalyticsMemoryStream.ts`, `.test.tsx`).
- **State + ledger instrumentation:** `SessionStateSnapshot.refresh_analysis_inputs_manifest()` carries lane-reuse metadata so chart-only revisions still show SQL receipts as reused, and new tests cover reuse + plan propagation (`backend/analytics/core/session_state.py`, `backend/tests/analytics/test_session_state_receipts.py`, `backend/tests/analytics/test_revision_routing.py`).
- **Web research question bundles:** `build_web_research_questions`, `perform_response_search`, and `WebRetrieverAdapter` now generate deterministic user/industry prompts (with `question_kind` tagging), persist them via `SessionStateSnapshot.record_web_research_questions`, surface them in `web_ready` / `web_revision_ready` SSE payloads, and hydrate specialist cards/tests (`backend/analytics/services/response_search.py`, `backend/analytics/flows/tooling.py`, `backend/tests/analytics/test_response_search.py`, `backend/tests/analytics/test_web_retriever_adapter.py`).
- **Baseline hydration + guardrails:** `SessionStateSnapshot.ensure_analysis_outputs_from_revision` seeds narrative/chart receipts from revision snapshots/artifacts, `analytics_memory_workflow` blocks revisions whenever `has_analysis` is false, SingleAgentController emits structured `missing_analysis` SSEs/tests, and the frontend now renders the matching follow-up banner (`backend/analytics/core/session_state.py`, `backend/analytics/flows/workflow.py`, `backend/analytics/flows/single_agent_tools.py`, `backend/tests/analytics/test_session_state_receipts.py`, `backend/tests/analytics/test_revision_routing.py`, `components/analytics/hooks/useAnalyticsMemoryStream.ts`, `.test.tsx`, `components/analytics/types.ts`).
- **Multi-agent API parity:** `MultiAgentFlow.analysis_revision` / `run_analysis_refresh` / `run_web_refresh` accept `requested_focus`, persist Gemini bundles, and mirror web research questions on the session snapshot with coverage in `backend/tests/analytics/test_web_research.py`.
- **Revision instrumentation gating + guardrails (Nov 17, 2025 @ 14:05Z):** `analytics_memory_workflow` disables `instrument_events(...)` whenever `revision_requested=True`, SingleAgentController streams `agent_coordination`, `agent_turn_*`, and `tool_start|end` plus a `revision_agent_disabled` SSE instead of replaying the deterministic planner when the Agents SDK is offline, and `pytest backend/tests/analytics/test_single_agent_stream_events.py` proves the guardrail is emitted.
- **Multi-agent agentic revisions + UI telemetry (Nov 17, 2025 @ 18:45Z):** `MultiAgentFlow.run_analysis_refresh/analysis_revision` now force the supervisor orchestrator whenever a revision directive is agentic, emit `agent_coordination` + `agent_turn_*` events, stream `revision_agent_disabled` guardrails when the supervisor runtime is unavailable, persist those runs via `SessionStateSnapshot.record_agent_run`, and expose the guard status across the UI (`backend/analytics/flows/multi_agent.py`, `backend/tests/analytics/test_multi_agent_flow.py`, `components/analytics/hooks/useAnalyticsMemoryStream.ts`, `.test.tsx`).

### Ledger QA (November 17, 2025 @ 22:05Z)
- Investigated `agent-process-ledger - 2025-11-16T233656.889.json` (single-agent) and `agent-process-ledger - 2025-11-16T233528.225.json` (multi-agent). Pre-fix SSE streams replayed the entire planner pipeline around the targeted revision stack; gating `instrument_events(...)` addressed the duplication. Multi-agent ledgers still lack `agent_*` and `tool_execution` entries, so follow-up verification is queued under Remaining Work.

## Remaining Work
All outstanding deliverables live here so controller, UI, and ops stakeholders can execute against one queue.

### Agentic Revision Remediation Plan (Open Items)
1. **Manual smoke test of the new flow badges:** Run a fresh multi-agent revision in the UI, capture screenshots of LiveArtifacts + ProcessPanel showing the telemetry-driven badges/themes, and attach the ledger hash so support can point to customer-visible evidence.
2. **Planner-executor coverage:** Once the backend emits `flow_mode` for deterministic SQL flows, replay a recent `docs/agent-process-ledger` capture through the new `useAnalyticsSqlStream` flowMode wiring and store the Vitest output to prove the SQL panel stays in sync with telemetry.

